[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ace-framework"
version = "0.1.0"
description = "ACE (Agentic Context Engineering) local-first framework MVP"
requires-python = ">=3.9"
dependencies = []

[project.scripts]
ace = "app.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
